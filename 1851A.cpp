#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, m, k, H;
    cin >> n >> m >> k >> H;
    int ht[n];
    for (int i = 0; i < n; ++i)
    {
        cin >> ht[i];
    }
}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }
}